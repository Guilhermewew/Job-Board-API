# Copie este arquivo para .env e ajuste conforme necessário.
# Se DATABASE_URL não for definida, a API usa SQLite local (./job_board.db).

DATABASE_URL=postgresql://jobboard:jobboard@localhost:5432/jobboard
